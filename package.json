{
  "name": "stooq-cache-api",
  "version": "1.0.0",
  "description": "Fetch Stooq data using Cache API to bypass KV and Cron",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
